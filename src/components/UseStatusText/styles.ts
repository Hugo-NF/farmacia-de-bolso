// Package imports.

import { TextProps } from 'react-native';
import styled from 'styled-components/native';

export const styledComponents = {

  InUseText: styled.Text<TextProps>`
    font-size: 20px;
    font-family: Roboto;
  `,

};
